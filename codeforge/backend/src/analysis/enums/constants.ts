export enum Severity {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
  INFO = 'info',
}

export enum WorkerType {
  SECURITY = 'security',
  COMPLEXITY = 'complexity',
  TEST_GAPS = 'test-gaps',
  BREAKING = 'breaking',
}
